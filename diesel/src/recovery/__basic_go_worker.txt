package main

import (
  "fmt"
  "math"
  "strconv"
  "os"
  "dieseldistacc"
)

func nroot(n float64, root int) float64 {
  return math.Pow(n, 1.0/float64(root))
}

const ArraySize = 10
const Num_threads = 2

var Input [ArraySize]float64

const (
  // single whitespace character
  ws = "[ \n\r\t\v\f]"
  // isolated comment
  cmt = "#[^\n\r]*"
  // comment sub expression
  cmts = "(" + ws + "*" + cmt + "[\n\r])"
  // number with leading comments
  num = "(" + cmts + "+" + ws + "*|" + ws + "+)([0-9]+)"
)

__GLOBAL_DECS__

__FUNC_DECS__

func main() {
	tid, _ := strconv.Atoi(os.Args[1])

  fmt.Println("Starting program");
 
  // using this s#!t because go complains when math not used
  _ = math.Inf
  
  __START__THREADS__(tid)

}
